const { send } = require("./main/apis/client");

async function a() {
    await send("hriwthik", "gqwaut", 10, 2, 0.3);
    await send("asqwedsa", "gaasdut", 100, 20, 3);
    await send("hriqwethiasdask", "gadasdsadsadsaut", 23, 20, 0.1);
    await send("hqwer", "aut", 12, 2, 2);
    await send("hrit", "gt", 100, 20, 10);
    await send("hrqweithik", "gaut", 10, 2, 0.3);
    await send("asdsdfsa", "gaasdfsdut", 100, 20, 3);
    await send("hrsfdithiasdask", "gadasdsadsdfsadsaut", 23, 20, 0.1);
    await send("sdfhr", "ausdft", 12, 2, 2);
    await send("hrsdfit", "gsfdt", 100, 20, 10);
}

a();